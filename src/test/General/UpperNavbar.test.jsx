import { screen, render } from "@testing-library/react";
// import UpperNavfbar from "../../../components/General/UpperNavbar";
import { BrowserRouter } from "react-router-dom"

describe("testing UpperNavbar component",()=>{
    test("rendering UpperNavbar correctly",()=>{
        // render(
        // <BrowserRouter>
        // <UpperNavbar name="upper"/>
        // </BrowserRouter>
        // );
        // const navbarName = screen.getByText("upper");
        // expect(navbarName).toBeInTheDocument();
        // const backArrow = screen.getByTestId("backArrow");
        // expect(backArrow).toBeInTheDocument();
    });
});